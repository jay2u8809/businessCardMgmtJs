
// Absolute value calculation
exports.abs = (number) => {
    return (0 < number) ? number : -number;
};

// Calculate the area of a circle
exports.circleArea = (radius) => {
    return radius * radius * Math.PI;ß
};